name = "Auto Off"
variant = "autooff"
description = "turns equipment off when all dependents are also off"
initial_mode = "ready"
watch = ["i2c3", "i2c4"]
settle = "PT10S"  # duration for "on" command to settle before notifying caller of completed

[equipment]
  name = "i2c5"
  impact = "power"
